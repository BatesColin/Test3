$(function(){
  $("#form-one").submit(function(event){
  event.preventDefault();
  var input = parseInt($("input#input").val());
  var inputLength =input.inputLength;
  console.log("input")
  var outPut = []
  var inputClone = inputNumber
  var number1.indexof[input]
  console.log(number1)

  //var inputNumber = concat.input
  for(inputNumber=inputNumber+1; inputNumber>0; inputNumber--){
    console.log(inputNumber)
    var outPut1 =toString(inputClone-1)
    if(outPut1.includes(3){
      outPut.replace(outPut,"I'm sorry, Dave. I'm afraid I can't do that.")

    }elseif(outPut1.includes(1){
      outPut.replace(outPut,"Boop!")

    }elseif(output1.includes(0){
      outPut.replace(outPut, "Beep!")
    }

    }

  }
  //for(index<inputLength; var input=0: index++){
  //for(var index = input; index <= inputLength; index++){
});
});



// var output = input.map(input(input) {
// outPut.push(number-1);
